<%= form_with(model: noa_weather_station) do |form| %>
  <% if noa_weather_station.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(noa_weather_station.errors.count, "error") %> prohibited this noa_weather_station from being saved:</h2>

      <ul>
        <% noa_weather_station.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
